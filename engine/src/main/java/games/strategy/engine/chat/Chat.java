package games.strategy.engine.chat;

/**
 * Placeholder for the desktop in-game chat. The mobile engine has no multiplayer, so a chat is
 * never created; the type only exists to keep the game loader signatures unchanged.
 */
public final class Chat {
  private Chat() {}
}
